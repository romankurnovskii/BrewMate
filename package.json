{
  "name": "BrewMate",
  "version": "1.0.4",
  "description": "BrewMate - Homebrew App GUI Manager for macOS",
  "author": "Roman Kurnovskii",
  "main": "dist/index.js",
  "scripts": {
    "start": "electron .",
    "start:dev": "NODE_ENV=development npm start",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "build": "tsc && npm run build:renderer && npm run copy:static",
    "build:renderer": "tsc src/renderer/renderer.ts --outDir dist/renderer --target ES2020 --module none --lib ES2020,DOM --skipLibCheck",
    "copy:static": "mkdir -p dist/renderer && mkdir -p dist/assets && cp src/renderer/index.html dist/renderer/ && cp src/renderer/styles.css dist/renderer/ && if [ -d src/assets ]; then cp -r src/assets/* dist/assets/; elif [ -d assets ]; then cp -r assets/* dist/assets/; fi",
    "build:mac": "npm run build && electron-builder --mac",
    "test:local": "./scripts/test-local-build.sh",
    "build:mas": "./scripts/build-mas.sh",
    "validate:mas": "./scripts/validate-mas-package.sh",
    "pre-submit": "./scripts/pre-submit-check.sh",
    "submit": "./scripts/submit-to-appstore.sh",
    "check:testflight": "./scripts/check-testflight-status.sh",
    "diagnose:launch": "./scripts/diagnose-app-launch.sh",
    "dist": "npm run build && electron-builder",
    "postinstall": "electron-builder install-app-deps"
  },
  "devDependencies": {
    "@electron/fuses": "^2.0.0",
    "@types/jest": "^29.5.12",
    "@types/node": "^24.10.0",
    "dotenv": "^17.2.3",
    "electron": "^39.0.0",
    "electron-builder": "^26.0.12",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.6.2",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,css,md}": [
      "prettier --write"
    ]
  }
}
